# DentalProject

A modern and responsive dental clinic website with online appointment booking, showcasing dental services, treatments, and patient information.

This repository was bootstrapped with Vite and React. Run `npm install` and `npm run dev` to start the development server.

